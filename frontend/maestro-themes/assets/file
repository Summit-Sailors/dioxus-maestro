// The assets directory will hold static assets for examples
// The preview_templates directory will hold the preview templates for theme preview
// Default themes would hold themes that users can start working with (Presets)